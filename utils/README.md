# Utilities

This folder is reserved for small pure helpers that can be shared by feature modules without coupling to UI or API concerns.

Current MVP keeps most existing helpers in `lib/utils.ts` for backward compatibility.
